@model IEnumerable<ProjectMVC.Models.Image>

@{
    ViewData["Title"] = "List";
}

<h1>房源管理</h1>

<p>
    <a asp-action="Create">新增房源</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ImageId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ImagePath)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ImageCaption)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ImageId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ImagePath)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ImageCaption)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.ImageId/* id=item.PrimaryKey */ }) |
           
                @Html.ActionLink("Delete", "Delete", new { id = item.ImageId/* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
